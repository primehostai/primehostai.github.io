media: docs/media
content:
  - name: docs
    label: Documentation
    type: collection
    path: docs
    filename: "{primary}.md"
    view:
      fields: [ title, order ]
      primary: title
      sort: [ order ]
      default:
        sort: order
        order: asc
    fields:
      - { name: title, label: Title, type: string, required: true }
      - { name: description, label: Description, type: string, options: { maxlength: 580 }}
      - { name: order, label: Order, type: number }
      - { name: body, label: Body, type: rich-text }